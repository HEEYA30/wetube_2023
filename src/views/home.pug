extends base.pug
include mixins/video

block content 
        each video in videos
            +video(video)
        else    
            li sorry nothing found.
            //- input(placeholder="what's your name?")
            //- button send
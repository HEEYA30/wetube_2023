extends base

block content 
    //- h3=video.views + views
    //- h3 #{video.views} #{video.views === 1 ? "view" : "views"}
    //- div
    //-     p=video.description
    //-     small=video.createdAt
    //- a(href=`${video.id}/edit`) Edit Video &rarr;

    block scripts
        script(src="/static/js/videoPlayers.js")